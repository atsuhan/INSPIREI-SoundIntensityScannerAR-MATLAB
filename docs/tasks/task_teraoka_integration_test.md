# タスク: 仮実装を実データで動くコードに書き換える

**担当: 寺岡**

---

## 背景

Claudeで解析パイプラインの設計と仮実装を作りました。
まだ実データでは動作確認していないので、**実際の計測データで動くコードに書き換えてください。**

## 設計ドキュメント

作業前に必ず読んでください。この設計に沿って書き換えます。

- `CLAUDE.md` — プロジェクトのルールと命名規則
- `docs/architecture.md` — ディレクトリ構成とデータフロー図

## やること

1. 計測データを `projects/2022_impreza_engine/data/` に配置する
2. `startup.m` → `runAnalysis.m` を実行して、エラーが出る箇所を特定する
3. `functions/` 内の関数を、実データで動くように書き換える
4. 最終的に `runAnalysis.m` が通しで動いて、以下の成果物が出力されるようにする

## 最終成果物

- **3Dベクトル場プロットの画像** — `.png` で保存
- **解析結果データ** — `.mat` で保存

保存先は `projects/2022_impreza_engine/` 直下でOKです。

## PR

ブランチを切って作業し、PRを作成してください。
PRの最後に以下を書いてください:

> チームの皆さんチェックをお願いします

## 完了条件

- `runAnalysis.m` がエラーなく最後まで実行できる
- `.png` と `.mat` が保存されている
- PRが作成されている
